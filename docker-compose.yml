services:
  paper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "25565:25565"
      - "8765:8765"
    environment:
      EULA: "TRUE"
    volumes:
      - paper-data:/server/logs
      - ../results:/results

volumes:
  paper-data:
